  <div class="d-flex justify-content-between align-items-center mb-3">
    <h2>Edit Loan Type</h2>
    <%= link_to("Back", {:action => 'index'}, :class => 'btn btn-secondary') %>
  </div>

  <%= render 'form', loan_type: @loan_type %>