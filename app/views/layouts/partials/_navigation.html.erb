<nav class="navbar navbar-expand-lg fixed-top navbar-light bg-dark">
  <div class="container">
    <%= link_to 'FINTRA', root_path, class: 'navbar-brand text-warning font-weight-bold' %>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav ml-auto">
        <% if user_signed_in? %>
          <li class="nav-item">
            <%= link_to 'Edit Profile', edit_user_registration_path, class: 'nav-link text-warning' %>
          </li>
          <li class="nav-item">
            <%= link_to 'Log out', destroy_user_session_path, method: :delete, class: 'nav-link text-warning' %>
          </li>
        <% else %>
          <li class="nav-item">
            <%= link_to 'Login', login_path, class: 'nav-link text-warning' %>
          </li>
          <li class="nav-item">
            <%= link_to 'Signup', signup_path, class: 'nav-link text-warning' %>
          </li>
        <% end %>
      </ul>
    </div>
  </div>
</nav>
